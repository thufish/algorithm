	for (int i=0;i<=str.size()-1;i++)
	{
		int strat_pos=i;
		char max_so_far=0;
		for (int j=str.size()-1;j>=i;j--)
		{
			if (str[j]>max_so_far)
			{
				max_so_far=str[j];
				strat_pos=j;
			}
		}
		i=strat_pos;
		str_result+=max_so_far;
	}
